# Use Case Service

:::info

Make sure to install the Docker Desktop App.

:::

Clone the Use Case Service repository from the dev branch and start the docker-compose.yml in it.

```tsx
docker-compose up -D
```